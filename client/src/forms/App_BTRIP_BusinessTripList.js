/**
 * @param {D5TableForm} form
 * @param {D5Core} core
 */
export const App_BTRIP_BusinessTripList = (form, core) => ({
  onInitForm() {

  }
});

window.userScript = {
  ...window.userScript,
  App_BTRIP_BusinessTripList
};
